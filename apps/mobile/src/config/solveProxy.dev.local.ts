/**
 * Device dogfood solve-proxy endpoint.
 * `scripts/phone-demo-proxy-mac.sh` overwrites url/token locally.
 * Keep empty in git — do not commit LAN URL or token.
 */
export const solveProxyDevLocal: { url: string; token: string } = {
  url: '',
  token: '',
};
