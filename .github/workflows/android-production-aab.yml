# Android production AAB via EAS local on GitHub Actions
# (avoids Expo cloud build quota + owner Mac disk pressure)
#
# Owner: Actions → "Android production AAB" → Run workflow
# Secrets required (repo Settings → Secrets and variables → Actions):
#   EXPO_TOKEN
#   EXPO_PUBLIC_FIREBASE_API_KEY
#   EXPO_PUBLIC_FIREBASE_APP_ID
# Optional (if app needs them at runtime):
#   EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN
#   EXPO_PUBLIC_FIREBASE_PROJECT_ID
#   EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET
#   EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID

name: Android production AAB

on:
  workflow_dispatch:

concurrency:
  group: android-prod-aab-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-aab:
    name: eas build --local (production)
    runs-on: ubuntu-latest
    timeout-minutes: 120
    defaults:
      run:
        working-directory: apps/mobile

    env:
      EAS_BUILD_PROFILE: production
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      EXPO_PUBLIC_FIREBASE_API_KEY: ${{ secrets.EXPO_PUBLIC_FIREBASE_API_KEY }}
      EXPO_PUBLIC_FIREBASE_APP_ID: ${{ secrets.EXPO_PUBLIC_FIREBASE_APP_ID }}
      EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN }}
      EXPO_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.EXPO_PUBLIC_FIREBASE_PROJECT_ID }}
      EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET }}
      EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
      EAS_LOCAL_BUILD_WORKINGDIR: ${{ github.workspace }}/.eas-local-build
      # Avoid interactive prompts
      CI: 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify required secrets
        working-directory: .
        run: |
          missing=0
          for key in EXPO_TOKEN EXPO_PUBLIC_FIREBASE_API_KEY EXPO_PUBLIC_FIREBASE_APP_ID; do
            if [ -z "${!key}" ]; then
              echo "::error::Missing secret: $key"
              missing=1
            fi
          done
          exit $missing

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: apps/mobile/package-lock.json

      - name: Free runner disk (pre-SDK)
        working-directory: .
        run: |
          sudo rm -rf /usr/share/dotnet || true
          df -h

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install mobile deps
        run: npm ci

      # postinstall should already strip foojay; re-run so EAS archive is safe
      # even if npm skipped scripts or node_modules was restored from cache.
      - name: Patch RN foojay (Plugin Portal bypass)
        run: node scripts/patch-rn-gradle-foojay.js

      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: EAS local production AAB
        env:
          # Prefer setup-java JDK; do not auto-download toolchains via Foojay.
          ORG_GRADLE_PROJECT_reactNativeArchitectures: arm64-v8a
          GRADLE_OPTS: >-
            -Dorg.gradle.java.installations.auto-download=false
            -Dorg.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
        run: |
          mkdir -p "$EAS_LOCAL_BUILD_WORKINGDIR"
          # Defense in depth: EAS copies node_modules into the local build dir;
          # ensure the patch is present on the workspace copy before pack.
          node scripts/patch-rn-gradle-foojay.js
          eas build \
            --platform android \
            --profile production \
            --local \
            --non-interactive \
            --output "$GITHUB_WORKSPACE/cozbil-production.aab"

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: cozbil-android-production-aab
          path: cozbil-production.aab
          if-no-files-found: error
          retention-days: 14

      - name: Disk after build
        if: always()
        working-directory: .
        run: df -h
