rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Phase 1 stub: deny all until Phase 2 user-scoped paths
      allow read, write: if false;
    }
  }
}
