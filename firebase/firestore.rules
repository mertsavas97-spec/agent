rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Phase 1 stub: deny all until Phase 2 auth rules land
      allow read, write: if false;
    }
  }
}
