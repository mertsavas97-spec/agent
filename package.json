{
  "name": "cozbil",
  "private": true,
  "version": "1.0.0",
  "description": "ÇözBil — LGS / YGS / KPSS AI study companion",
  "scripts": {
    "typecheck": "npm run typecheck --prefix apps/mobile && npm run typecheck --prefix functions",
    "lint": "npm run lint --prefix apps/mobile && npm run lint --prefix functions",
    "test": "npm run test --prefix apps/mobile && npm run test --prefix functions",
    "mobile": "npm run start --prefix apps/mobile",
    "functions:build": "npm run build --prefix functions",
    "qa": "npm run typecheck && npm run lint && npm run test"
  },
  "engines": {
    "node": ">=20"
  }
}
